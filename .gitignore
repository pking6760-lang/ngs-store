# Dependencies
node_modules/

# Build output
dist/
build/
www/

# Capacitor generated web assets (regenerated by `npm run sync:android`)
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json

# Android SDK path
android/local.properties

# Logs
*.log

# Environment
.env
.env.local

# OS
.DS_Store

# Single-file preview build
preview/

# Built Android APKs (build artifacts — delivered directly, not versioned)
*.apk

# Backend secrets (all env files except the example)
.env
.env.*
!.env.example
!.env.production
!.env.development

# Firebase CLI deploy cache
.firebase/

# NGS Partner Android project — ignore build output (like android/)
android-partner/build/
android-partner/app/build/
android-partner/.gradle/
android-partner/capacitor-cordova-android-plugins/build/
android-partner/app/src/main/assets/public/
www-partner/
www-customer/
scratchpad/

# NGS customer Android project — ignore build output
android-customer/build/
android-customer/app/build/
android-customer/.gradle/

# Release signing (kept out of git — delivered separately)
signing/

# The customer APK we host for download on the website
!public/ngs.apk
